#######################################################################################
# HYPRLAND CONFIG — GRUVBOX DARK HARD (DARK TONES ONLY)
#######################################################################################

################
### MONITORS ###
################
monitor = DP-1,2560x1440@180hz,auto,1

###################
### MY PROGRAMS ###
###################
$terminal = kitty
$fileManager = nnn
#$menu = bemenu-run -i --bottom -p ">" --single-instance -n --margin 10 
$menu = ~/.config/hypr/bemenu-gruvbox.sh


#################
### AUTOSTART ###
#################
exec-once = waybar
exec-once = mako

#test for vpn
exec-once = gnome-keyring-daemon --start --components=secrets



#############################
### ENVIRONMENT VARIABLES ###
#############################
env = XCURSOR_THEME,Bibata-Original-Classic
env = XCURSOR_SIZE,18

#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 0.5
    gaps_out = 0
    border_size = 2

    # Subtle, dark Gruvbox tones
    col.active_border = rgb(689d6a)   # dark_aqua
    col.inactive_border = rgb(0f1010) # dark background tone (between bg1 and bg2)

    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

decoration {
    rounding = 0
    rounding_power = 0

    active_opacity = 0.98
    inactive_opacity = 0.94

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(0f1010dd)  # bg0_hard
    }

    blur {
        enabled = false
        size = 3
        passes = 1
        vibrancy = 0.15
    }
}

animations {
    enabled = true

    bezier = easeOutQuint, 0.23, 1, 0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear, 0, 0, 1, 1
    bezier = quick, 0.15, 0, 0.1, 1

    animation = global, 1, 3, default
    animation = border, 1, 1, easeOutQuint
    animation = windows, 1, 1, easeOutQuint
    animation = fadeIn, 1, 1, quick
    animation = fadeOut, 1, 1, quick
    animation = workspaces, 1, 1, easeOutQuint, fade
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}

#############
### INPUT ###
#############
input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0
    accel_profile = flat
    touchpad {
        natural_scroll = false
    }
}

gesture = 3, horizontal, workspace

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

###################
### KEYBINDINGS ###
###################
$mainMod = SUPER

# Screenshot a region
bind = , PRINT, exec, hyprshot -m region --clipboard only

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod, P, pseudo,
bind = $mainMod, J, togglesplit,
bind = $mainMod, L, exec, hyprlock

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrulev2 = suppressevent maximize, class:.*
windowrulev2 = nofocus, class:^$, title:^$, xwayland:1 floating:1 fullscreen:0 pinned:0


##############################
### THEME COLORS (DARK ONLY) ###
##############################

# Core dark Gruvbox tones
# bg0_hard = #0f1010
# bg1      = #1d2021
# bg2      = #282828
# bg3      = #3c3836
# bg4      = #504945
# fg2      = #d5c4a1
# dark_red     = #cc241d
# dark_green   = #98971a
# dark_yellow  = #d79921
# dark_blue    = #458588
# dark_purple  = #b16286
# dark_aqua    = #689d6a
# dark_gray    = #a89984
# dark_orange  = #d65d0e

# Recommended usage across setup:
# - Backgrounds: #0f1010 or #1d2021
# - Borders: #3c3836
# - Accents: #689d6a or #d79921
# - Text: #d5c4a1
# - Alerts (urgent): #cc241d

